[lxc]
mirror.debian: http://localhost:3142/ftp.de.debian.org/debian
# an alternative would be (if you don't use apt-cacher-ng or a transparent (apt) proxy) http://ftp.de.debian.org/debian
mirror.ubuntu: http://localhost:3142/archive.ubuntu.com/ubuntu
# an alternative would be (if you don't use apt-cacher-ng or a transparent (apt) proxy) http://archive.ubuntu.com/ubuntu

[lxc-apt]
# an URL of an apt-proxy to use (note that DNS in the LXC isn't supported, i.e. specify an IP or a domain name which can be resolved by an external DNS)
apt.proxy: http://richter-local.de:3142

